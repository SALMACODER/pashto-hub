/**
 * Backwards-compatible re-export.
 * New code should import from './client' or one of the resource modules
 * (./auth, ./books, ./dictionary, ./lessons) instead of './axios'.
 */
export { default } from './client'
